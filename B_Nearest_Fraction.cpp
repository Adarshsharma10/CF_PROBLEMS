#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define all(v) (v).begin(), (v).end()



int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int x,y,n;
    cin>>x>>y>>n;
    double fr = x/y;
    int a = 0;
    for(int b=1;b<=n;b++){
        
    }

    return 0;
}

// Pending